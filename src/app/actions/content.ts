import {type} from '../utilities/action_types';

export const ContentActionTypes = {
    /*VIEW_CONTENT: type('[CONTENT] ViewContent'),
    VIEW_PROFILE: type('[CONTENT] ViewProfile'),
    ADD_CONTENT: type('[CONTENT] AddContent')
*/
}
