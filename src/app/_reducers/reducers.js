"use strict";
function userReducer(state, action) {
    if (state === void 0) { state = null; }
    switch (action.type) {
        default:
            return state;
    }
}
exports.userReducer = userReducer;
function avatarReducer(state, action) {
    if (state === void 0) { state = null; }
    switch (action.type) {
        default:
            return state;
    }
}
exports.avatarReducer = avatarReducer;
function profileReducer(state, action) {
    if (state === void 0) { state = null; }
    switch (action.type) {
        default:
            return state;
    }
}
exports.profileReducer = profileReducer;
function contentReducer(state, action) {
    if (state === void 0) { state = []; }
    switch (action.type) {
        default:
            return state;
    }
}
exports.contentReducer = contentReducer;
;
function newContentNumberReducer(state, action) {
    if (state === void 0) { state = null; }
    switch (action.type) {
        default:
            return state;
    }
}
exports.newContentNumberReducer = newContentNumberReducer;
;
